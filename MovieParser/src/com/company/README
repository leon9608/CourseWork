The user should write two required parameters and a third parameter if necessary on the command line
There should be space between each parameter.

<# movies>  <query type>  [query parameter]

1. # movies is the number of movies that you want to see at the return line and it must be positive integers
2. four query types : displayAll
                     getSpecificGenre
                     getVoteAverageAbove
                     getPopularityAbove
 3. query parameter for getSpecificGenre should be positive integers
    query parameter for getVoteAverageAbove should be positive number between 0 to 10
    query parameter for  getPopularityAbove should be positive number
